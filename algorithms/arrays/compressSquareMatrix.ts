export default function compressSquareMatrix(
    matrix: number[][],
    factors: number[],
    x: number
) {
    let sum = -1;
    for (const row of matrix) {
        console.log(row);
    }
    return sum;
}
