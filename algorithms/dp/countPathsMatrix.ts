/**
 * Count all possible paths from 0,0 to m-1,n-1 of a matrix of size m:n.
 * @param input integer 2D array.
 */
const countPathsMatrixRecursive = function (input: number[][]) {};

export function countPathsMatrixTab() {}

function generateInput() {}

export default function main() {
    const input: number[][] = [];
    const cache = [];
    const currentResult = countPathsMatrixRecursive(input);

    return currentResult;
}
